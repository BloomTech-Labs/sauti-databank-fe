import React from "react";

const GraphButtons = () => {
  const lineButton = () => {};

  return (
    <>
      {/* <button className="line-btn" onClick={() => setOpen("line")}>
          <img src={graphImage} />
          <h1>Line Graph</h1>
        </button> */}
      'Hello'
    </>
  );
};
export default GraphButtons;
